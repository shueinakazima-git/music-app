# Database Configuration
# Copy this file to .env and update with your actual database credentials

# set DB_TYPE to "oracle" (default) or "postgres" to switch database drivers
#DB_TYPE=oracle

# ---------- Oracle configuration (if using Oracle) ----------
DB_USER=system
DB_PASSWORD=Password123
DB_CONNECT_STRING=localhost/FREEPDB1

# ---------- PostgreSQL configuration (if using Postgres) ----------
#PG_USER=postgres
#PG_PASSWORD=Password123
#PG_HOST=localhost
#PG_PORT=5432
#PG_DATABASE=musicapp
